package loader;

import (
)


/* Generates full PE file, assuming shellcode has already been encrypted with AES */
func GetPE(shellcode []byte, aesKey []byte) ([]byte, error) {


    return aesKey, nil
}
